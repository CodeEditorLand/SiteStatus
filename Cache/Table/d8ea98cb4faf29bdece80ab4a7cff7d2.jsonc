// CACHE: GET /repos/{owner}/{repo}/commits | owner=CodeEditorLand | repo=Mountain | per_page=5 | page=15 → GET /repos/{owner}/{repo}/commits.RESPONSE.map(Author,Date,File,Message,SHA)
{
	"Set": [
		{
			"Author": "Nikola Hristov",
			"Date": "2026-06-12T18:48:24Z",
			"File": [
				"⠀⠀⠀⠀⠀",
				1858,
				5
			],
			"Message": "docs(Mountain): Rewrite rustdoc comments to meet quality standards\n\nReplace meta-instructional doc text (\"This struct does X\" → \"X\", \"Represents Y\" → \"Y\"), remove empty and blank doc-comment lines, and convert TODO-section module stubs into proper //! module documentation with structured lists.\n\nChanges:\n- `MountainEnvironment.rs`: Condensed struct-level doc to remove \"Primary dependency injection container\" preamble in favour of a direct description.\n- `CocoonService/mod.rs`: Promoted stale \"thin-wrapper dispatcher\" ASCII comment to proper //! module doc with formatted item list; rewrote struct- and field-level docs to drop \"This struct\" / \"Provides\" meta-text.\n- `RPC/mod.rs`: Expanded module doc to a formatted //! block.\n- `rewrite_docs.py`, `fix_rustdocs.py`: Added automation scripts for ongoing rustdoc quality enforcement across the IPC and core modules.",
			"SHA": "725b11e0cfe12294c5830848c3cd74d0efa0d32f"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-06-12T18:24:00Z",
			"File": [
				"⠀⠀⠀⠀⠀",
				1856,
				1
			],
			"Message": "chore(Mountain): Remove stale .commit-message.json artifact\n\nThe `.commit-message.json` file at the Mountain root was left behind from the commit message generation workflow and contains the message text of the most recent commit (3e14d7e8). This file has no purpose outside the generation pipeline and should not be tracked.\n\nUnstage and delete the artifact.",
			"SHA": "b991e9aa7f464743fe1aa8efc9d545504871e0cf"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-06-12T18:23:20Z",
			"File": [
				"⠀⠀⠀⠀⠀",
				1855,
				1
			],
			"Message": "feat(Mountain): Bridge WebSocket frames to MountainIPCInvoke dispatch\n\nRegister a default handler on the Mist handler registry that bridges every\ninbound `{ method, params }` WebSocket frame into the\n`crate::IPC::WindServiceHandlers::mountain_ipc_invoke` dispatch path — the\nsame Tauri command used by the Sky frontend — so the full\nWindServiceHandlers surface is reachable over the Mist WebSocket transport\nwithout per-method registration.\n\nThe handler normalises params into a Vec<Value> (Array passes through,\nNull becomes empty, scalars wrap into single-element) and delegates to the\nexisting mountain_ipc_invoke. The Mist secret is wrapped in Some() for\nconsistent optional semantics on ServeLocal.\n\nThis eliminates the staged handler-registration plan described in the\nprevious comment: the fallback makes every method available immediately.",
			"SHA": "3e14d7e8b3d31fb0c80ff943a64015d2d7d3f3d0"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-06-12T18:22:25Z",
			"File": [
				"⠀⠀⠀⠀⠀",
				1855,
				2
			],
			"Message": "feat(Mountain): Add CancelOnDrop guard to provider forward calls\n\nReplace the direct `IPCProvider::SendRequestToSideCar` in `InvokeProvider`\nand `InvokeProviderMethod` with `ForwardCancellable`, a wrapper that runs\nthe Vine call on a detached `tokio::spawn` task guarded by a drop-triggered\nwatch signal. A new `CancelOnDrop` guard flips the signal when the calling\nfuture is dropped — from gRPC cancellation, typed-rail `CancelOperation`,\npeer disconnect, or any caller-side `select!` loss — so the detached task\ndelivers `CocoonService.CancelOperation` for the wire request id before\nexiting, terminating the side-car provider instead of computing a result\nnobody is waiting for.\n\nBoth provider-invocation paths now use `FnCancellable` from the Vine\nclient instead of the old blocking `SendRequestToSideCar`, eliminating\nthe `Arc<dyn IPCProvider>` / `Require()` dependency in both modules.",
			"SHA": "c1eb47dc38cbaf90ea0132444cba790bf24f046b"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-06-12T18:20:51Z",
			"File": [
				"⠀⠀⠀⠀⠀",
				1855,
				6
			],
			"Message": "feat(Mountain): Mount CocoonService on shared port, invalidate extension install cache, pipe session ID to Cocoon\n\nMount `CocoonServiceServer` (typed `CocoonServiceImpl`) on the same tonic router as `MountainServiceServer` so both services share port 50051, replacing the previous unused-variable pattern. Derive `MAX_MESSAGE_SIZE` from `Vine::Client::Shared::MAX_MESSAGE_SIZE_BYTES` for lockstep configuration across the client-server boundary.\n\nCall `BumpScanGeneration()` on extension install and uninstall so that cached `extensions:getInstalled` responses from an earlier scan generation are invalidated, preventing stale builtins-only manifests from being served after user-path extensions are added or removed.\n\nInject `CocoonSessionId` into the Cocoon sidecar environment, carrying the same session UUID used for `telemetryInfo.sessionId` in initialization data. This lets `vscode.env.sessionId` resolve correctly in Cocoon before the extension-host handshake completes. Promote `SessionId()` from `fn` to `pub(crate) fn` for cross-module access.\n\nReplace the `dev_log`-based subscriber-lag warning in `VineSubscribeCommand` with a call to `Vine::Client::Shared::ReportNotificationLag`, upgrading a local log message to structured instrumentation that consumers can act upon.",
			"SHA": "7723552a9ff1ccd21397cddd7058838a788c775e"
		}
	],
	"TimeStamp": 1783777325676
}