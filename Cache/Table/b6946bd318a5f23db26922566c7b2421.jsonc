// CACHE: GET /repos/{owner}/{repo}/commits | owner=CodeEditorLand | repo=Element | per_page=5 | page=30 → GET /repos/{owner}/{repo}/commits.RESPONSE.map(Author,Date,File,Message,SHA)
{
	"Set": [
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-20T06:14:14Z",
			"File": [
				"⡏⠀⠀⠀⠀",
				26,
				3
			],
			"Message": "refactor(Project): Extract Cocoon namespace maps, Mountain dispatchers\n\nAdvance three Element submodules with structural extractions, runtime fixes, and formatting alignment.\n\n**Cocoon** lifts the inline RegisterCustomEditor function and six shared registry maps (CustomEditorProviders, CustomEditorProvidersByViewType, TreeDataProviders, TreeDataProvidersByViewId, WebviewViewProviders, WebviewViewBuilders, WebviewPanels) from Namespace.ts into dedicated RegisterCustomEditor.ts and Registry.ts modules. Namespace.ts shrinks by ~170 lines and re-exports the relocated maps through the existing path, preserving all provider registration and lifecycle behaviour unchanged.\n\n**Mountain** extracts the 1400-line `process_mountain_request` and 600-line `send_mountain_notification` match blocks from CocoonService/mod.rs into GenericRequest::Dispatcher and GenericNotification::Dispatcher modules, reducing mod.rs by ~1900 lines and clarifying the gRPC service boundary. Three fixes accompany the refactor: (1) scope the MutexGuard on WorkspaceFolders to a block so it is dropped before any await point, restoring Send-ness for tauri::async_runtime::spawn; (2) replace the OnceLock that caches a permanent None (routing all dev logs to /tmp after the first failed resolution) with a two-phase RESOLVED/RETRY system that retries after Tauri creates the app-data directory; (3) replace a bare `return Ok(...)` inside a WindServiceHandlers match arm (which exited the enclosing async block and broke Scheduler::Submit's `Output = ()` bound) with an if/else expression. A clippy-nit removing redundant `.as_str()` in AliasPath.rs is included. Two follow-up style commits normalize dispatcher indentation and update Cargo.toml/tauri.conf.json build identifiers for the dev profile.\n\n**Worker** normalises indentation across all three ESBuild configuration files to hard tabs via two style-only commits (spaces → tabs, then tabs → fully aligned tabs). Zero functional changes — build output is identical.\n\nAll Target/ compiled artifacts and RouteManifest files are regenerated for Cocoon.",
			"SHA": "a74c89e53e1c6310ff031e05dafc682fd0b0c121"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-20T05:38:35Z",
			"File": [
				"⡏⠀⠀⠀⠀",
				26,
				3
			],
			"Message": "feat(Project): Extract Cocoon extension host, wire hierarchy RPC, fix IPC\n\nAdvance Cocoon, Mountain, and Sky submodules in a coordinated round-trip that decomposes the extension host monolith, adds missing IPC endpoints for call/type hierarchy, and fixes data accuracy across the whole pipeline.\n\n**Cocoon** splits the ~1500-line inline extension host handler into three focused modules — VscodeModuleHooks.ts, EnsureVscodeAPI.ts, ActivateExtension.ts — reducing Handler.ts to a thin dispatch layer. Five real-world extension compatibility issues are fixed alongside the refactor: Copilot dist-preflight so missing build artifacts don't crash activation; `context.extension.packageJSON` populated from the real manifest on disk (`@vscode/extension-telemetry` reads `aiKey`); workspace config cache seeded from `contributes.configuration.properties` before `activate()` runs (prevents GitLens TypeError on nested config access); `activate()` return value backfilled into the registry so `vscode.extensions.getExtension(id).exports` resolves; POJO-tolerant `RelativePattern` wrapper added (fixes Shopify.ruby-lsp activation). Six missing VS Code API enums (`LogLevel`, `OverviewRulerLane`, `UIKind`, `TextEditorCursorStyle`, `DebugConfigurationProviderTriggerKind`, `IndentAction`) are now defined — extensions referencing them at module-load time crashed with undefined. `willSaveTextDocument` now batches `waitUntil` thenables into a single `window.applyTextEdits` call matching VS Code's `acceptWillSaveDocument` ordering. Configuration cache supports a `\"*\"` sentinel for full model rebuild on bulk settings writes.\n\n**Mountain** adds language-scoped configuration overrides — `UpdateValue` writes into `\"[<langId>]\"` scoped keys matching the VS Code convention used by prettier-vscode, vscode-eslint, and other per-language formatters. A `OnceLock`-backed static session ID replaces per-call `Uuid::new_v4()`, so extensions comparing `telemetryInfo.sessionId` across initialization calls see a consistent process-lifetime value. `CocoonService` dispatches `$provideCallHierarchyItems`/`prepareCallHierarchy` and `$provideTypeHierarchyItems`/`prepareTypeHierarchy` to the respective providers, unblocking F12 / Go to References / Call Hierarchy in the editor. Three `mountain_*` IPC stubs return real data: `mountain_get_configuration` yields the live merged config, `mountain_get_services_status` reports actual Cocoon connection state, `mountain_get_state` surfaces real workspace folder count and active document URI. SOCKS proxy env-var parsing emits the correct PAC keyword (`SOCKS` for socks URIs, `PROXY` for http/https). Dev-log dedup switches from `lock()` to `try_lock()` so a contended mutex never parks a Tokio worker. Always-on-top toggle moves from a heuristic title-prefix proxy to a `static AtomicBool`, correctly flipping state on each invocation. The `window.didChangeTextEditorSelection` Vine notification now includes `viewColumn` from incoming arguments, so extensions calling `activeTextEditor.viewColumn` see the correct pane number in split-editor layouts. A follow-up formatting commit normalizes brace placement and comment reflow across four files.\n\n**Sky** replaces the hardcoded `viewColumn: 1` in `sky:editor:activeChanged` and `sky:editor:selectionChanged` IPC payloads with a `GetViewColumn()` helper that reads `EditorGroups.activeGroup.index` (0-based) and converts to the 1-based view column the VS Code API expects, safely falling back to 1 when the EditorGroups service isn't yet available.\n\nExtension host reliability and IPC data accuracy are materially improved across all three layers of the round-trip.",
			"SHA": "3e397f73d617f23b3da7e170f7405fd4af1b8176"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-20T05:22:09Z",
			"File": [
				"⡏⠀⠀⠀⠀",
				26,
				3
			],
			"Message": "fix(Project): Advance 3 element submodules with Cocoon live state, Mountain IPC hardening, and Sky bridge decomposition\n\nAdvance Land/Element submodules in a coordinated round-trip spanning the extension host, native backend, and editor UI layer.\n\n**Cocoon** replaces stale/empty state placeholders with live-tracking counterparts that extensions query at runtime. `MakeUriStub` provides a minimal URI when `LazyURI.parse` fails, preventing folders from being silently dropped from the workspace list. `__visibleTextEditors` is now upserted on `didChangeTextEditorSelection` and filtered on `$acceptModelRemoved`, so GitLens/Error Lens/formatters see real open-editor state. `createTerminal` pushes into `__terminals` with proper dispose cleanup. A module-level `_AllDiagnostics` cache mirrors every `DiagnosticCollection.set/delete/clear` call, and `getDiagnostics()` returns real aggregated diagnostics with `diagnostics.didChange` event emission.\n\n**Mountain** delivers three fixesets: (1) file write handlers now update metadata and notify Cocoon of configuration changes via `configuration.change` Vine notification, so extensions' `ConfigCache` invalidates the affected key rather than serving stale data. (2) `StorageWriteDebouncer` race resolved by reordering the flush loop to take the pending item before clearing `FlushScheduled`; the workspace-scoped debouncer is now correctly selected instead of always routing through the global debouncer; low-level IPC noise (`file:open`, `file:close`, `textFile:save`) suppressed via skip list; Cocoon-connect boot latency eliminated by removing the 3-second blocking `WaitForClientConnection` from `languages:getAll`/`languages:getEncodedLanguageId` handlers. (3) Lock contention reduced in `ScanAndPopulateExtensions` by swapping an N-insert loop for a single pointer-swap, and in `DiagnosticProvider` by snapshotting under the lock then aggregating outside it.\n\n**Sky** decomposes ~2500 lines of inline handler registrations in `Bridge.ts` into four dedicated module files via dynamic imports: `InstallWebview.ts` (webview lifecycle, custom editors), `InstallEditorOperations.ts` (decorations, text edits, selections, reveal), `InstallTreeView.ts` (data provider attachment with exponential backoff retry), and `InstallUiRequests.ts` (show-message/input-box/quick-pick dialogs). Each module receives only its required dependencies through a thin injector interface, eliminating global-scope closure capture across unrelated handlers.",
			"SHA": "21c3681b99864ae1e8b02c4bdd3cf7358015edd5"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-20T05:10:23Z",
			"File": [
				"⡏⠀⠀⠀⠀",
				26,
				3
			],
			"Message": "feat(Project): Advance 3 element submodules with save notifications, Undo/Redo routing fix, and IPC bridges\n\nAdvance Land/Element submodules in a coordinated update that extends the editor save lifecycle, fixes macOS keyboard routing, and adds three new Sky IPC bridges.\n\n**Mountain** fires `$acceptModelSaved` via Vine after successful disk writes in both `FileWriteNative` and `TextfileWrite`, enabling Cocoon to dispatch `onDidSaveTextDocument` to subscribed extensions (format-on-save, organize-imports, save listeners). The save is fire-and-forget (tokio::spawn) to prevent Vine IPC failures from cascading to the workbench caller. Both handlers gain `dev_log` instrumentation for write latency and byte count. On macOS, removes Undo/Redo from the native Edit submenu via `AppMenu::SetAppMenu` so Cmd+Z and Cmd+Shift+Z reach Monaco's keydown handler instead of being intercepted by AppKit's responder chain. Non-fatal; logs a warning and keeps the default menu if the override fails. No-op on Windows/Linux.\n\n**Sky** adds three bridge implementations: `InstallDiagnostics.ts` routes diagnostics from Mountain to `IMarkerService` for in-editor error visibility; `InstallScm.ts` mirrors SCM state and registers providers against the live workbench service; `InstallSearch.ts` routes file and text search queries to Mountain's handlers.\n\n**Maintain** regenerates rustdoc output against Rust nightly 1.97.0, updating static asset hashes, search index entries, crates.js, and inline source references to match the current toolchain.",
			"SHA": "c29b18affb77f31c56e100bb5d0e38f49e9126e1"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-20T04:50:41Z",
			"File": [
				"⣿⣿⡟⠀⠀",
				26,
				14
			],
			"Message": "fix(Project): Advance 14 element submodules with Mountain fixes, Air cleanup, and ecosystem maintenance\n\nPush 14 Land/Element submodules forward in a coordinated chore/fix round-trip. The headline changes are Mountain's runtime fixes and Air's dead-code removal, with the remaining updates spanning documentation regeneration, dependency bumps, license alignment, and build manifest polish.\n\n**Mountain** fixes four classes of issues: (1) shell PATH inheritance so git/Node/language servers resolve after Finder/Dock launch when `IsTty()` is false; (2) git-availability caching to prevent a transient false from the first probe (fired before PATH is extended) from permanently disabling the SCM UI; (3) removal of prematurely-introduced `prepare_call_hierarchy`/`prepare_type_hierarchy` gRPC handlers and import-path disambiguation (`Msg` aliases, qualified `MountainVinegRPCService`, `RunTime()` method call, `MetricType` alias); (4) gating `tauri_build::build()` behind `CARGO_BUILDING_DOCS` so `cargo doc` succeeds without Sky Astro assets, plus Brotli pre-bake as `beforeBundleCommand` and a `Record` bump to 1 in Info.plist.\n\n**Air** removes 98 lines of stale dead code (`ApiVersion` struct and `ApiVersionManager` left from the modularization extract) and adds `#[derive(Debug)]` to `RetryManager`, `CircuitBreaker`, `BulkheadExecutor`, and `ResilienceOrchestrator` for structured logging. A follow-up fix removes the ambiguous `pub use ApiVersion::*` re-export that shadowed the new module name.\n\n**Sky** changes `licenseName`/`licenseUrl` in `Public/product.json` from MIT to CC0-1.0, aligning editor metadata with the NLnet NGI0 Commons Fund public-domain positioning.\n\n**Grove** replaces inline `license = \"MIT\"` with `license-file = \"LICENSE\"` in Cargo.toml so the full MIT license text ships with the published crate.\n\n**Cocoon** regenerates `RouteManifest.ts` (timestamp advanced to 2026-05-20T04:19:17Z, 135 Mountain-side methods) and rebuilds Target/ artifacts with no source-level changes.\n\n**SideCar** regenerates rustdoc HTML for Rust nightly 1.97.0 (up from 1.96.0), updating CSS/JS asset hashes and const-unstable tracking annotations.\n\n**Common**, **Echo**, **Mist** advance their Documentation/Rust submodule pointers. **Common** also merges a dependabot bump for `@cloudflare/workers-types` 4.20260519.1.\n\n**Rest** syncs its submodule pointer. **Output** and **Wind** merge their Current branches with no new commits. **Maintain** advances with a clean merge commit.\n\nThis round-trip stabilizes Mountain's runtime environment and IPC handlers, eliminates dead code in Air, aligns license metadata across the project, and keeps all documentation regenerated against the latest Rust nightly.",
			"SHA": "fc00b94313503ab7e593390310505ef6d4079183"
		}
	],
	"TimeStamp": 1783777328608
}