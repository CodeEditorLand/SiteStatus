// CACHE: GET /repos/{owner}/{repo}/commits | owner=CodeEditorLand | repo=Element | per_page=5 | page=10 → GET /repos/{owner}/{repo}/commits.RESPONSE.map(Author,Date,File,Message,SHA)
{
	"Set": [
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-26T18:07:05Z",
			"File": [
				"⣿⣿⡇⠀⠀",
				26,
				13
			],
			"Message": "fix(Land): Advance submodules — Cocoon doc shim, Mountain SCM replay, Output Tauri 2 polyfills, Sky panel routing\n\nAdvance all 13 Element submodules with substantive fixes across Cocoon, Mountain, Output, and Sky, along with a coordinated telemetry field rename and routine documentation submodule updates.\n\n**Cocoon** (`fix`): BuildTextDocumentShim in Notification/Handler returns a full TextDocument-shaped object backed by DocumentContentCache, fixing TypeError crashes in vscode.git diff decorations and vscode.merge-conflict that called `.getText()` on bare `{uri, fileName}` stubs. Fix scmHandle/groupHandle routing in Scm/Namespace so multi-repo SCM updates reach the correct provider. Fix showTextDocument URI resolution for all argument shapes. Rename telemetry config fields PostHog OTLPEndpoint→Pipe, OTLPEnabled→Emit. RouteManifest grows from 140 to 142 RPC methods with `git.openChange` and `git.openFile`.\n\n**Mountain** (`refactor` + `fix`): Replay SCM resource group registration and update events for late SkyBridge subscribers — persist ResourceStates as SourceControlManagementResourceDTO entries keyed by `(ProviderHandle, GroupId)`, then reconstruct GroupHandle at replay time through the provider identifier map. Add replay legs for `sky://scm/registerGroup` and `sky://scm/updateGroup` so the SCM panel populates at boot. Fix LocalPTYGetProfiles Arguments→args casing so terminal profile arguments are parsed correctly. Set the Trace sentinel to explicit categories instead of the catch-all `\"all\"`. Add window management capabilities (start-dragging, toggle-maximize, etc.) enabling the Sky titlebar to drive native chrome. Align SCM DTO import path with Common's reorganized module layout.\n\n**Output** (`fix` + `feat`): Align all ProcessTauri command names to snake_case — Tauri 2 passes the literal Rust function name without auto-camel conversion. Rename update dispatch names to match Mountain's service handler arms (`update:check`→`update:checkForUpdates`, etc.). Expose IOutputService in the CEL accessor so Sky can write to the actual Output panel. Refactor InjectTauriDragRegion from a runtime stylesheet walker (which returns empty strings under WKWebView) to build-time CSS regex extraction, baking drag/no-drag selector arrays into the bundle. Add capture-phase mousedown listener that walks the ancestor chain and dispatches Tauri invoke commands with multiple fallback paths, since Tauri's own drag listener only activates under `tauri://localhost`.\n\n**Sky** (`fix`): Route sky://output/append/clear/replace through `__CEL_SERVICES__.Output.getChannel().append()` instead of the diagnostic console sink, so panel content actually renders. Fix multi-repo SCM deduplication by keying ScmShimRegistry on `scmId#handle` instead of bare `scmId`, giving each repository its own workbench provider. Add snapshot serialization and early-return guards to visibleChanged/tabsChanged handlers, suppressing redundant `$acceptVisibleEditorsChanged` callbacks to Cocoon. Rename OTLP env vars to Pipe/Emit.\n\n**Wind** (`chore` + `refactor`): Remove unused model:updateContent IPC route. Rename Tauri invoke targets `VineSubscribeNotifications`→`vine_subscribe_notifications` and `VineSubscriberCount`→`vine_subscriber_count` to match Mountain's snake_case registration.\n\n**Common, Worker**: Coordinated OTLPEndpoint→Pipe / OTLPEnabled→Emit field rename across Configuration (Rust) and Bridge.ts (TypeScript).\n\n**Air, Echo, Grove, Mist, Rest, SideCar**: Routine Documentation/Rust submodule pointer advances — no functional changes.",
			"SHA": "4a4cfa76166d31a917575eef07e78315a18b806f"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-26T15:17:34Z",
			"File": [
				"⠇⠀⠀⠀⠀",
				26,
				2
			],
			"Message": "feat(Land): Advance submodules — Output Tauri 2 fix, Sky extensions refresh\n\nAdvance Output and Sky Element submodules with a runtime command-name fix for Tauri 2 and a live Extensions view refresh after extension lifecycle events.\n\nOutput renames the four Shared Process health-check commands from PascalCase (`CocoonExtensionHostHealth`, `CocoonSearchServiceHealth`, `CocoonDebugServiceHealth`, `SharedProcessServiceHealth`) to snake_case to match Mountain's registered handler names. Tauri 2 passes the literal Rust function name as the command identifier without the auto-camel conversion present in Tauri 1, causing `Command not found` errors at runtime. All four per-service health checks now resolve correctly under Tauri 2's strict command-name matching.\n\nSky adds `RefreshExtensionsView`, a guard that invokes `workbench.extensions.action.refreshExtension` after Mountain emits `extensions:install` (VsixInstaller success) or `extensions:uninstall`. VS Code's `IExtensionGalleryService` install pipeline exposes no public hook accessible from outside Cocoon's process boundary, so the command is called programmatically via `Commands.executeCommand`. The refresh is wired after both `sky://extensions/installed` and `sky://extensions/uninstalled` handlers with a try/catch wrapper so a failing command never disrupts the DOM CustomEvent dispatch reserved for Wind orchestration listeners. The Extensions sidebar now reflects newly installed or removed extensions live without requiring a workbench reload.",
			"SHA": "2c5e32336374ec29160c40d6fe8da6badfd735dd"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-26T15:11:53Z",
			"File": [
				"⡟⠀⠀⠀⠀",
				26,
				4
			],
			"Message": "refactor(Land): Advance submodules — Output build-time drag selectors, Wind codegen, Cocoon rebuild, Mountain dep bump\n\nAdvance four Element submodules with a build-time refactor in Output, daily codegen regeneration in Wind, a compiled-output rebuild in Cocoon, and a CI dependency bump in Mountain.\n\nOutput reworks InjectTauriDragRegion from a runtime stylesheet walker to a build-time CSS source scanner. Since WebKit drops the Chromium-only `-webkit-app-region` declaration at parse time, runtime CSSStyleRule inspection returns empty strings under WKWebView. The new approach extracts drag/no-drag selectors via regex from VS Code's CSS source files on disk at build time and bakes them into the workbench bundle as `globalThis.__LAND_DRAG_SELECTORS__` / `__LAND_NO_DRAG_SELECTORS__` arrays. The runtime polyfill switches from walking stylesheets to iterating these pre-collected lists with querySelectorAll, with an early bail when both arrays are empty. Companion changes: remove the left: 80px drag-region CSS inset from MacTitlebarOffsetCSS (the HTML attribute polyfill handles Tauri hit-testing), add padding-right: 0 assertion to prevent right-edge titlebar drift, improve the triple-signal fullscreen detector, and gate InjectPartZIndexCSS behind DisableUIFixes.\n\nWind regenerates all generated artifacts after upstream VS Code source corrections — CommandCatalog drops from 551 to 550 entries, ServiceCatalog grows from 500 to 502 (adding IChatViewFactory and ISessionsPartService), ISessionsManagementService gains a setActive method, and four per-service upstream files have corrected source line references.\n\nCocoon rebuilds Target/ artifacts after RouteManifest regeneration. Mountain bumps ad-m/github-push-action from 1.1.0 to 1.3.0 via Dependabot.",
			"SHA": "0f281e4508bba88675bb19ea30c7f5ff353fa3ca"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-26T14:18:27Z",
			"File": [
				"⠃⠀⠀⠀⠀",
				26,
				1
			],
			"Message": "refactor(Output): Replace hardcoded drag selectors with stylesheet-driven extraction\n\nThe InjectTauriDragRegion polyfill previously maintained a brittle hardcoded list of titlebar drag and no-drag CSS selectors that required manual updates whenever VS Code upstream changed its titlebar or sidebar markup.\n\nReplace the selector list with a runtime stylesheet walker that discovers which elements VS Code already declares as `-webkit-app-region: drag` / `no-drag` in its CSS rules and stamps the equivalent `data-tauri-drag-region` attribute dynamically. The walker recurses into CSSMediaRule and CSSSupportsRule containers so OS-scoped and feature-gated rules are picked up automatically. Selectors are cached after the first full pass and reused across MutationObserver callbacks; a second collection runs on `window.onload` to cover lazy-loaded extension stylesheets. Cross-origin sheets that throw on `cssRules` access are skipped defensively, and selectors that fail `querySelectorAll` (e.g. `:has()` / `:where()` on older WebKit builds) are caught per-selector without aborting the pass.\n\nThis eliminates the need to manually update the polyfill whenever VS Code upstream changes its drag-region markup — the polyfill now reflects whatever the stylesheets declare.",
			"SHA": "d223e6fdba417a840b75561804779492dcb3ca8a"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-26T14:04:35Z",
			"File": [
				"⡏⠀⠀⠀⠀",
				26,
				3
			],
			"Message": "feat(Land): Advance submodules — Mountain refactor, Output polyfills, Wind regeneration\n\nAdvance Mountain, Output, and Wind Element submodules with architectural cleanup, editor UI polyfills, and daily codegen regeneration.\n\nMountain reverts the binary, library, and package names from the verbose encoded build-configuration string back to the canonical \"Mountain\", reducing developer friction in local tooling and debugging. Tauri resource entries are reordered for consistency, the missing shell-integration/zsh.zsh resource is added, and the identifier is restored to fiddee.editor.binary. Stale .Backup artifacts from the encoding revert are removed.\n\nOutput introduces two Tauri 2 editor UI polyfills. InjectTauriDragRegion stamps data-tauri-drag-region attributes on titlebar elements (with data-tauri-drag-region=\"false\" on interactive children) so native window-drag hit-testing works with the overlay-titlebar configuration, with a MutationObserver for late-mounted regions. The MacTitlebarOffsetCSS polyfill is extended with a triple-signal fullscreen detector (HTML5 fullscreen API, display-mode media query, dimension heuristic) that toggles body.land-fullscreen to reclaim 80px left padding when macOS native fullscreen hides the traffic-light cluster, with explicit padding-right: 0 assertion. InjectPartZIndexCSS is gated behind DisableUIFixes to prevent menubar and dropdown clipping under specific configurations.\n\nWind regenerates the full set of generated artifacts — CommandCatalog (551 entries), ServiceCatalog (500 entries), per-service Upstream interfaces for ~500 services extracted from the VS Code source tree, and 16 bridge shapes (Activity, Clipboard, Command, ContextKey, Dialog, Editor, Extension, Host, Keybinding, Lifecycle, Notification, Product, Progress, Settings, Storage, Workspace) — all carrying the DO NOT EDIT BY HAND header.",
			"SHA": "e966e663b4509584f9ee265f86de4301e0ffd4e7"
		}
	],
	"TimeStamp": 1780506233848
}