// CACHE: GET /repos/{owner}/{repo}/commits | owner=CodeEditorLand | repo=Mountain | per_page=5 | page=69 → GET /repos/{owner}/{repo}/commits.RESPONSE.map(Author,Date,File,Message,SHA)
{
	"Set": [
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-02T00:43:59Z",
			"File": [
				"⠁⠀⠀⠀⠀",
				886,
				20
			],
			"Message": "refactor(Mountain): Modularize cache layers into PascalCase submodules\n\nDecompose `AssetMemoryMap` and `PathCanon` from monolithic source files into dedicated submodule trees (`AssetMemoryMap/` and `PathCanon/`) to align with the project-wide PascalCase naming convention and improve modularity.\n\nExtract logical units (Entry, Map, Stats, LoadOrInsert, etc.) into separate files within their respective cache directories. Rename all public types and functions to strict PascalCase (e.g., `Entry` → `Entry::Struct`, `LoadOrInsert` → `LoadOrInsert::Fn`, `CacheStats` → `CacheStats::Struct`). Update `Binary/Build/Scheme.rs` and `Environment/Utility/PathSecurity.rs` to consume the new modular APIs.\n\nThis structural refactor preserves all runtime behavior (mmap asset serving, path canonicalization with moka eviction) while establishing a consistent namespace hierarchy for future cache expansion.",
			"SHA": "2a8ec534f8bd476cbf86dae0727b044f778bc569"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-02T00:08:20Z",
			"File": [
				"⠀⠀⠀⠀⠀",
				868,
				2
			],
			"Message": "perf(Mountain): Coalesce tree-view emits and canonicalize webview payloads\n\nExtend the 16ms coalescing pattern to the `RegisterTreeViewProvider` RPC in `Source/RPC/CocoonService/TreeView.rs`. During extension boot, 30+ tree view registrations previously saturated the Tauri IPC channel. The new `TreeViewEmitBatch` singleton accumulates payloads and emits a single batch per frame, preserving the `sky://tree-view/create` channel while deferring to `sky-emit` logging for diagnostics.\n\nStandardize `webview` IPC payload shapes in `Source/Track/Effect/CreateEffectForRequest/Webview.rs`. Fix a critical bug where positional arguments (`[Handle, Value]`) from Cocoon were wrapped in `{ method, handle, args }`, causing SkyBridge's listeners (expecting `viewId` or `html` keys) to reject payloads, leaving iframes blank. The new logic detects wire formats, extracts named fields, and normalizes camelCase method names (e.g., `setHtml`) to kebab-case channel suffixes (`set-html`) to match the `Common` library definitions. Ensure both webview-panels and webview-views render correctly for extensions like Roo and claude-vscode.",
			"SHA": "6d8f8d296641459e47b373e39f865cfbe6b2219c"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-02T00:07:51Z",
			"File": [
				"⠀⠀⠀⠀⠀",
				868,
				3
			],
			"Message": "refactor(Mountain): Standardize IPC variable and identifier names to PascalCase\n\nComplete the project-wide naming convention migration by renaming all local variables and parameter identifiers in the IPC layer (`Connection/Manager.rs`, `TauriIPCServer.rs`, `TauriIPCServer/Server.rs`) from camelCase to PascalCase.\n\nRename `handle` → `Handle`, `message` → `Message`, `app_handle` → `app_handle` (struct field remains, local param updated to `AppHandle` where applicable), and generic error bindings like `e` → `Error` in documentation and code comments. Update all associated doc strings, example code blocks, and logging statements to reflect these changes. Also normalize terminology in comments (e.g., 'message' → 'Message', 'router' → 'Router') to align with the strict PascalCase convention specified in CLAUDE.md for all identifiers, including local variables within `impl` blocks and test modules.\n\nThis refactor ensures consistent code style across the Mountain backend IPC subsystem without altering any runtime logic or data flow.",
			"SHA": "e2f06c0d52af54f8abe630eacded288a29f29433"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-02T00:07:11Z",
			"File": [
				"⠀⠀⠀⠀⠀",
				868,
				4
			],
			"Message": "perf(Mountain): Deduplicate FS watchers and single-flight directory walks\n\nReduce kernel resource exhaustion and redundant I/O during extension boot by implementing deduplication strategies in the native environment layer.\n\nIn `FileWatcherProvider`, introduce a deduplication index keyed by `(root, recursive, pattern)` to prevent multiple extensions from spawning duplicate `notify::Watcher` instances for identical paths. Aliased handles now share the primary OS watcher, with events fanned out to all subscribers in the forwarder task.\n\nIn `WorkspaceProvider`, implement a single-flight registry for `findFiles` operations. Concurrent requests for the same search key now await a single filesystem walk leader rather than re-walking the tree, fixing the race condition where cache misses occurred before the leader populated the cache.\n\nIn `ConfigurationProvider`, add a 250ms in-memory cache for parsed `settings.json` files to eliminate redundant disk reads triggered by the `InspectConfigurationValue` handler. Invalidate the cache immediately upon configuration updates.\n\nThese changes significantly lower IPC saturation and CPU usage during the high-load extension activation phase.",
			"SHA": "44851c1cd73bfdaa2a743295959359a05e11a082"
		},
		{
			"Author": "Nikola Hristov",
			"Date": "2026-05-01T23:48:12Z",
			"File": [
				"⠃⠀⠀⠀⠀",
				868,
				53
			],
			"Message": "refactor(Mountain): Rename IPC handler bindings to PascalCase\n\nComplete the PascalCase migration started in the previous commit by renaming all local identifiers in WindServiceHandlers to match the project convention: runtime → RunTime, args → Arguments, app_handle → ApplicationHandle, and error bindings such as e → Error. Update doc comments and JSON field names that referenced the old identifiers.\n\nAlso fix a boot-time URI normalization bug in BuildInitialUrl: RecentlyOpened.json stores workspace URIs with a trailing slash (file:///.../Mountain/). Strip it before URL-encoding the folder path so that workbench-side URI.revive produces a folder URI matching URI.from, which never carries a trailing slash on the parent directory. The mismatch caused IUriIdentityService.extUri.relativePath to return absolute paths, making breadcrumbs, quick-pick, and Problems-panel labels render full /Volumes/... paths instead of the workspace-relative short form.",
			"SHA": "fd28a7bd6076ee9b112d8a1d8b5301c11b75bfc9"
		}
	],
	"TimeStamp": 1779241318442
}