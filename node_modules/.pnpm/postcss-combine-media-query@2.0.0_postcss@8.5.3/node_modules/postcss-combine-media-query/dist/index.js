"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
const postcss_1 = __importDefault(require("postcss"));
const plugin = (opts = {}) => {
    let atRules = {};
    function addToAtRules(atRule) {
        const key = atRule.params;
        if (!atRules[key]) {
            atRules[key] = postcss_1.default.atRule({ name: atRule.name, params: atRule.params });
        }
        atRule.nodes?.forEach((node) => {
            atRules[key].append(node.clone());
        });
        atRule.remove();
    }
    return {
        postcssPlugin: 'postcss-combine-media-query',
        Once(root) {
            root.walkAtRules('media', (atRule) => {
                addToAtRules(atRule);
            });
            Object.keys(atRules).forEach((key) => {
                root.append(atRules[key]);
            });
            atRules = {};
        },
    };
};
plugin.postcss = true;
module.exports = plugin;
